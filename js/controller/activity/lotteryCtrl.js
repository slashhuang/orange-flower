/**
 *   build by rwson 2015-09-14
 *   活动说明控制器
 */
define([], function () {
    function lotteryCtrl($scope, $routeParams, $location, $http, $rootScope) {

        $scope.uniqueClass = "";
        //  单独的class

    }

    lotteryCtrl.$inject = ['$scope', '$routeParams', '$location', '$http', '$rootScope'];
    return lotteryCtrl;
});